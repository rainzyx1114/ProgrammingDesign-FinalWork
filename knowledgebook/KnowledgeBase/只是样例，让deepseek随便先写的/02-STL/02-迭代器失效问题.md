# 02-迭代器失效问题

## 常见失效场景

### vector

```cpp
vector<int> v = {1, 2, 3};
auto it = v.begin();
v.push_back(4);  // 可能扩容，it 失效！
```

- push_back / insert → 可能扩容，所有迭代器失效
- erase → 被删除元素及之后所有迭代器失效

## 安全做法

```cpp
it = v.erase(it);  // erase 返回下一个有效迭代器
```